server:
  port:8080


spring:    
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AymanGhaly/spring-cloud-config-server
          

management:
  security:
    enabled:false